import React from "react";
import ReactDOM from "react-dom";

class ParcelExample extends React.Component {
  render() {
    return <h1>Create a React app with zero configuration using Parcel</h1>;
  }
}

ReactDOM.render(<ParcelExample />, document.getElementById("app"));
